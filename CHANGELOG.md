# Changelog

## Unreleased

- execute Round 5 direction-B headroom PILOT before building any trace-eval harness (pitfall #1: power-analyze first): 6 safety-critical multi-step scenarios, blind Sonnet, approach-(b) elicited ordered action traces, hand-scored vs oracle — result 6/6 PASS (0% fail vs the 30–40% target) → the action-trace surface SATURATES too; the bundle is now measured-good two ways (decision-JSON in R2–R4, action-plan traces here). Lane reaches its honest end: remaining headroom (B-approach-a real-execution drift, or direction C weaker operator) is operator-robustness, not bundle-quality (pitfall #9) — a separate project. Pilot saved the full B harness build. Seed + outcome `docs/plans/2026-06-13-001-direction-b-trace-level-eval-seed.md`; receipt `runs/20260613T053539Z-r5b-trace-pilot/`
- execute Round 4 — strengthen the AMB-1 fix by inlining the >50%-rewrite exclusion directly into the `ambient_learning` precedence-row trigger (R3's scoping carve-out was Sonnet-variance-prone ~6/8); v2-val-002 robust FAIL→PASS (5/5 surfaces, every pass 12/12, no channel-integrity re-run needed), **robust eval reaches 24/24**, zero regression (v1 18/18, sentinels clean, controls 4/4); PR #55 MERGED; verdict `runs/20260613T045832Z-r4-amb1-strengthen/R4_VERDICT.md`
- execute Round 3 targeted bundle repair (direction A) — Codex fresh-context leak-isolated author clarifies AMB-2 (gulf3 0c idempotence → tied to the existing Session Close decision row) + AMB-2b (reuse condition reconciled to `completed or skipped`); MIXED verdict, zero regression: AMB-2 (v2-test-006) repaired robust FAIL→PASS, AMB-1 (v2-val-002) not repaired on the Sonnet channel (variance) but cross-model Codex portability 5/5 = channel-variance not wording-defect; row-value byte-freeze + CX-5 drift-sentinels (caught/cleaned 2 single-pass Sonnet noise events via target-blind re-runs) + CX-8 Codex diff review all green; PR #54 MERGED; verdict `runs/20260613T033910Z-r3-repair/R3_VERDICT.md`, lessons `runs/pitfalls-r3.md`
- execute Round 2 T2–T4 + amendment A end-to-end: author the 30-item v2 eval set (panel-verified, dedup'd vs v1) plus 96 frozen paraphrase variants (k=4, 4 registered axes, diversity receipt); extend the v2 validator with an arbitrary-surface leak scan (check 6) and full variant guards (check 7); add the fail-closed comparator copy (`--pass-label` + exact id-set matching) and the fail-closed robust aggregator — 13 synthetic failure cases verified; calibration exits terminal `blocked_eval_headroom` under BOTH metrics (exact-primary 1→0→0; robust k=4 iter1 = 1 failing vs band 3–6) — load-bearing finding: dual-metric saturation of the Sonnet+bundle channel
- open PR #53 committing the v2 `seed_manifest.json` (23 hash-pinned files incl. the tool chain) + `.gitignore` v2 negations; v2 becomes the second regression suite; W-A/B/C correctly withheld per parent Decision 1; Round 2 verdict at `runs/round2-verdict-2026-06-12.md`, lessons at `runs/pitfalls-r2.md`
- lock the Round 3 bundle-repair plan (direction A: the two measured ambiguities + a Codex-found third inconsistency) after duo review — eng 3 findings + Codex 8 (REJECT → rev2 → 8/8 CONFIRMED-FIXED → APPROVE); amendment A itself duo-reviewed earlier the same day (eng 5 + Codex 12 across 3 passes, converged APPROVED); paper trail under `reviews/2026-06-12-*`
- commit the validated Decision Contract baseline (PR #51): `### Decision Contract Quick Reference` table inside `autorefine/SKILL.md > Preflight`, Decision Contract / Mutation Split Policy definitions + lane history in `CONTEXT.md`, and a `runs/` ignore block with a negation that keeps the seed manifest committable
- commit the content-addressed seed manifest for the salvaged Darwin/SkillOpt eval dataset (PR #52): SHA-256 × 308 files; data stays untracked but revalidatable; split-contract + semantic-leak validators and a fail-closed pre-launch data gate live as untracked tooling under `runs/seed-data/tools/`
- execute Round 1 of the Fable 5 Darwin/SkillOpt e2e loop (Run 0 probe, Run A real round, Run B resume drill, Run C cap drill) — engineering verdict PASS with one recorded PARTIAL; key finding: the v1 eval set is saturated by the integrated baseline (18/18 before any mutation); receipts in untracked run roots, verdict at `runs/round1-verdict-2026-06-11.md`
- commit the Round 2 duo-review paper trail (`reviews/2026-06-11-round2-*`) and apply-back gate TODOs (`TODOS.md`); the Round 2 plan itself stays local-only per the `docs/` exclude + pre-commit governance hook
- stage (working tree, ships with the v2 manifest PR) `.gitignore` negations for `runs/seed-data-v2/`; freeze the Round 2 workflow script `runs/workflows/darwin_skillopt_round.js` before any v2 oracle authoring (sha receipt alongside)

- consolidate AutoRefine's tangled **Run Context Recovery** instructions in `autorefine/SKILL.md`: collapse four divergent general-load hydration lines into one canonical, checkpoint-gated list and delete a second cluster of three redundant resume-deserialize lines, then add the previously-missing `edit_budget` to BOTH the general-load and `Step A` checkpoint-resume paths — recomputing `current_budget = max_edits` on load/resume per the edit-budget contract rather than trusting a persisted value — so a resumed Phase 7 run cannot silently drop its budget; add an anti-drift note pointing future fields at the single canonical list (and Step A); add `autorefine/tests/test_run_context_recovery_contract.sh` (16 prose contract assertions). Salvages the one genuinely valuable piece of the deprecated PR #44 (closed: stale, broke 7 tests on a trial-merge), re-cut cleanly against current `main`
- add a bounded **edit budget** (SkillOpt-inspired Step A regularizer) to the Phase 7 mutation loop: new nullable `state.json.edit_budget` field, initialized at iteration-start to `{schedule:constant, max_edits:3, current_budget:3}` so it is active by default; applied as a generation-time constraint (the proposer makes at most `current_budget` discrete changes per experiment — no post-hoc clip, since the candidate is a full `content` rewrite), preserved across checkpoint/resume with a `constant`-schedule `current_budget = max_edits` drift recompute, honored by every `challenger_mode` lane and the shortlist winner, recorded as `mutation.md > edit_budget_applied`, and read by a budget-aware content-ceiling circuit-breaker guard so a too-tight budget is not misdiagnosed as exhausted content; add `autorefine/tests/test_phase7_edit_budget_contract.sh` (18 prose/schema contract assertions)
- add `autorefine/scripts/render-phase7-status.py` — manual debugging tool that renders a Phase 7 status snapshot from `state.json` + run-dir artifacts to `[workspace]/phase7-status.md`. Pure stdlib; user invokes manually when they get lost mid-run.
- add `autorefine/lib/search_family_regression.py` and `autorefine/scripts/eval-search-family-regression.py` — pure-function library + thin CLI wrapper for per-query-family regression detection on `search_retrieval_v1` paired predictions, reusing the existing `score_predictions` scorer; emits `search_family_regression_v1` JSON with `pass`/`block`/`invalid` status, blocking-family detail, and stable exit codes 0/1/2
- extend the Phase 7 step 2c regression-check contract in `autorefine/references/gulf3-generalization.md` with a search-adapter family-regression sub-step that calls the new gate when `selected_adapter_id = "search_retrieval_v1"`, preserving the existing experiment-0/1 skip rule
- harden the Gulf 1 trace recorder and converter after code-review, TDD, security, and architecture review: HTTP/1.1 streaming, `/v1`-safe upstream joins, auth-header passthrough routing, official HTTPS upstream allowlist, default credential-like scrubbing, basename skill hints, stricter converter validation, and fail-closed output writes with explicit `--force`
- open PR #37 (`fix/autorefine-broken-tests`) for the live-trace capture path, mark it ready for review, resolve the `main` merge conflict, and confirm GitHub reports the PR as mergeable and clean with CI passing
- add `autorefine/scripts/build-search-silver.py` — deterministic synthetic silver-set builder for `search_retrieval_v1` that turns a frozen corpus into `query`/`doc_id`/`grade` rows with query-family split metadata and human-review status
- add `autorefine/scripts/eval-search-metric.py` — dependency-free `NDCG@k` / `recall@k` metric runner for ranked `doc_id` predictions, with per-query evidence and false-pass / false-fail diagnostics
- add Python regression coverage for the search silver-set builder and metric runner, including a live generator-to-scorer integration path and report-count guard for grade-0 hard negatives
- add `autorefine/scripts/run-campaign.py` — Phase 7-only campaign manifest validator, dependency/cluster scheduler, report-only skill adjacency/DRY audit, human-gated Gulf work packets, and plan-only top-level `execution_plan` reporting for prepared AutoRefine workspaces
- add `autorefine/scripts/prepare-gulf-gate-pack.py` — explicit advanced utility for adapter-backed deterministic Gulf 1 / Gulf 2 preauthorization without changing the default plan-only campaign workflow
- document the campaign orchestrator language tradeoff in `plan.md`: Python for V1 contract stabilization, with Rust or TypeScript deferred until the workflow proves useful
- add `autorefine/scripts/record.py` — local proxy recorder that captures Claude Code sessions to JSONL so Phase 0.5 comprehension can ingest real traces
- add `autorefine/scripts/records-to-gulf1.py` — JSONL-to-Gulf1 converter with heuristic classification (success/failure/do-not-trigger) preserving `source_trace` cross-reference
- document the canonical Gulf 1 Trace Record Schema under `autorefine/references.md` with `records/<skill_slug>/<session_id>.jsonl` layout
- wire Option D (record live sessions) into the Phase 0.5 contract wizard in `autorefine/references/gulf1-comprehension.md`
- add `description_quality.not_for_clause_hint` informational sub-signal — silent when do-not-trigger examples exist, suggests adding a "NOT for:" clause when both are absent
- document SkillClaw SKILL.md superset compatibility — Claude Code silently ingests `metadata.skillclaw.*` and related superset frontmatter
- restructure the public repo so `autorefine/` is the clear shipped bundle
- rewrite the public `README.md` and `autorefine/README.md` around product-vs-bundle roles
- add curated public user docs under `docs/`
- move internal design, handover, research, seed, and asset artifacts into the `dev` submodule
- move repo-maintenance shell tests out of the shipped bundle into `dev/tests/`
- add `.claude-plugin/` and `.codex-plugin/` manifests plus a plugin-facing README
- normalize internal note and archive naming in `dev/` and add an internal consolidation sanity check
- fix the shipped bundle README so standalone installs point at live GitHub docs instead of missing repo-relative paths, then re-verify the merged `main` bundle from a fresh scratch `~/.claude/skills/autorefine` install
