# Changelog

## Unreleased

- restructure the public repo so `autorefine/` is the clear shipped bundle
- move internal design, handover, and research artifacts into the `dev` submodule
- add curated public user docs under `docs/`
- move repo-maintenance shell tests out of the shipped bundle into `tests/contracts/`
