# Python build/cache
__pycache__/
*.pyc
.pytest_cache/

# Generated debugging artifacts
# phase7-status.md is regenerated from state.json + run artifacts every time
# autorefine/scripts/render-phase7-status.py runs. Workspace-local, never committed.
phase7-status.md

# Darwin/SkillOpt loop run state (docs/plans/2026-06-10-001, Step 0.5):
# run roots stay untracked; only the content-addressed seed manifest is committable.
runs/*
!runs/seed-data/
runs/seed-data/*
!runs/seed-data/seed_manifest.json
