# AutoRefine — Gulf 3: Generalization

Phase 7 + Session Close. Read when: Gulf 2 gate approved (or Quick Start returning), entering Phase 7 or Session Close.

Downstream phase-entry contract: on entry to Phase 7 or Session Close, initialize pattern-aware context from `state.json.phase1_context.selected_skill_pattern`. Downstream phase-entry contract: on entry to Phase 7 or Session Close, initialize pattern-aware context from `state.json.phase1_context.selected_skill_pattern` and `state.json.phase1_context.selected_eval_strategy_id`. Also initialize split-aware context from `state.json.mutation_stage_split_access_policy`. If only the persisted Phase 1 artifact is available, read the same canonical ID from the top-level `selected_skill_pattern` field in `design-audit.md` and hydrate the loaded run context before scoring, mutation analysis, or version comparison. If only the persisted Phase 1 artifact is available, read the same canonical IDs from the top-level `selected_skill_pattern` and `selected_eval_strategy_id` fields in `design-audit.md` and hydrate the loaded run context before scoring, mutation analysis, or version comparison. If only the pattern is available, resolve the missing strategy through `references.md > Skill Pattern Eval Strategy > Pattern-to-Evaluation-Strategy Selector` before continuing. If the active context does not already hold `mutation_stage_split_access_policy`, read the exact policy from `fixtures-manifest.md` or a stored Phase 4 `evaluation_metadata.config.mutation_stage_split_access_policy` snapshot and hydrate the loaded run context before scoring, mutation analysis, regression checks, version comparison, or any other dataset read. If the active context does not already hold the bootstrap meta-learnings context, resolve `state.json.meta_learnings_path` (or the default AutoRefine skill-directory copy), normalize the current target context, and hydrate `{meta_learnings_path, target_context, parsed_meta_learnings}` using `references.md > Campaign Bootstrap Meta-Learnings Context` before mutation analysis or Session Close steering. If the active context does not already hold `style_preferences`, rebuild the normalized payload from `state.json.mid_session_preference_signals` plus `state.json.mid_session_preference_signals_path` using `references.md > Style Preferences Payload` before scoring, mutation analysis, version comparison, or Session Close steering so every mid-loop stage sees the same preference envelope. Preserve the same bootstrap envelope's reporting fields — `curator_source`, `curator_version`, `transfer_parameters`, and `transfer_traceability` — whenever run output or retrieval payloads are serialized so downstream export/filter consumers can trace exactly which curated rules steered the run. Preserve the normalized `style_preferences` payload unchanged in retrieval/report payloads so downstream stage handoffs and production consumers can reuse the same explainable mid-session preference context without reparsing raw state. Once restored, route Phase 7 scoring, mutation analysis, and version comparison through the matching strategy row in `references.md > Skill Pattern Eval Strategy > Strategy Definitions`. Use that strategy route as the canonical mutation path instead of generic edits. Treat the restored mutation-stage policy as the active dataset-read gate for baseline scoring, mutation scoring, mutation analysis, regression checks, same-run version comparison, and resume-time dataset reopen. Treat `parsed_meta_learnings` from the bootstrapped context as the canonical cross-campaign steering payload for Phase 7. Treat `style_preferences.active_signals` as the canonical machine-readable intra-session preference ledger for the active run, and do not rescan raw override sources once that hydrated envelope is available. Stop and repair Phase 4 state if the policy is missing, malformed, or disagrees across sources. Do not trigger pattern classification again while entering Gulf 3; rerun Phase 1 Step 0 only if the persisted pattern is missing or mismatched with `state.json.skill_pattern`.

---

## Phase 6.5: Research Intake (optional)

Read `references.md > Research Intake Stage Contract` before doing any Phase 6.5 work.

Run this stage after Gulf 2 approval and before Phase 7 baseline whenever the user provides external references, a reference skill, or a resume checkpoint shows `state.json.research_intake.status` as `in_progress`, `completed`, or `partial`.

- Initialize the stage from `state.json.phase1_context.selected_skill_pattern` and `state.json.phase1_context.selected_eval_strategy_id`. Do not run a generic research pass without that pattern-aware context.
- Use the current workspace copy as the target (`[workspace]/skill-under-test/SKILL.md`) and require a one-sentence target domain for the skill-under-test before extracting anything from external sources.
- Resolve `research_intake_config` from the base defaults in `references.md > Research Intake Stage Contract`, then apply the pattern-aware source ranking for the active `selected_skill_pattern`, then apply any explicit `research_intake_overrides`. Record the final `resolution_mode` (`default_only`, `pattern_default_only`, or `explicit_override`) in `research-intake.md > Resolved Config` before validating sources.
- Respect the default source-selection controls unless an explicit valid override narrows or widens them within the contract ceilings: 4 total sources by default, 2 per source kind by default, all 5 supported source kinds enabled, normalized-location de-duplication, and no automatic remote discovery beyond the user-provided references.
- Respect the default retrieval limits unless a valid override changes them within the contract ceilings: target 3-5 extracted patterns per accepted source, require at least 1, cap the run at 12 total extracted patterns and 8 mutation leads, and keep `failure_focus[]` to at most 5 items.
- Validate every source against the contract before reading it. Reject duplicate, unreadable, or unsupported sources explicitly and record them in `research-intake.md` plus `state.json.research_intake`.
- For any remote `best_practice`, `article`, or `repo` source, fetch the exact source content into `[workspace]/research-sources/<source_id>/` before extracting patterns. Extract only from the stored snapshot, not from transient browser state or memory.
- Persist citation metadata, retrieval timestamps, and `raw_artifact_ref` for every fetched external source in both `research-intake.md` and `state.json.research_intake.accepted_sources`.
- If remote retrieval fails, reject that source explicitly instead of extracting from a partial fetch, placeholder response, or stale cached view.
- Keep `research-intake.md` human-readable, but normalize every persisted extracted pattern into one `pattern_observation` entry and every mutation lead into one `mutation_hypothesis` entry using `references.md > Research Corpus Normalization Contract`. Assign stable `entry_id`s and keep raw `source_kind` only as provenance.
- Fail fast with `error_code = "invalid_research_intake_config"` if an override tries to weaken same-skill targeting, Phase 1 context requirements, de-duplication, or holdout isolation.
- Write `[workspace]/research-intake.md`, set `state.json.research_intake_path`, and persist `state.json.research_intake = {status, target_skill_path, target_domain, requested_sources, accepted_sources, rejected_sources, completed_at, error_code}`.
- If the run ends with `state.json.research_intake.status = "completed"` or `"partial"`, Phase 7 may read `research-intake.md` as an optional mutation input. If the status is `"skipped"`, proceed with internal analysis only. If the status is `"failed"`, stop before Phase 7 and repair the research intake inputs instead of silently continuing as if research succeeded.
- Never use Research Intake to compare different skills as routing candidates, and never read `adversarial_holdout` or other hidden evaluation-only corpora from this stage.

---

## Phase 7: AutoResearch Loop

The Karpathy-style mutation-test-keep/discard cycle. Requires `eval-suite.md` + judges.

**Mode detection (check in this order):**
1. If both `gates.gulf_1` = "approved" AND `gates.gulf_2` = "approved" → **Full mode** (validated evals, confidence-weighted scoring). Budget: ask user — Quick (3), Standard (5), Deep (8-10).
2. Else if `quick_start.completed` = true AND both gates = "pending" → **Mini mode** (bootstrap evals, simplified weighting, directional labeling). Budget: 2-3 experiments.
3. Else → STOP and tell the user to run Standard pipeline first.

**Mini mode differences:** Fresh scoring corpus (5-10 generated inputs, NOT reusing Quick Start traces). Simplified weighting: code evals = 1.0, agent-as-judge = 0.5. All results labeled "directional." No confidence weighting from Phase 6 (evals aren't validated).

**The Loop:**

**Iteration directory (filesystem-as-memory):** At Phase 7 start, fire a single iteration-start trigger exactly once for the new run. That trigger creates the run directory `[workspace]/runs/run_<timestamp>/` (timestamp format: `YYYY-MM-DDTHH-MM-SS`), sets `run_id = run_<timestamp>`, writes the active run identifier to `state.json.current_run_id`, writes the run directory path to `state.json.current_run_path`, sets `state.json.current_experiment = 0`, and appends one run-start record to `results.json.iteration_runs[]` using `references.md > Iteration Run Record Schema`. Treat that same atomic write as the baseline-in-progress recovery state for the run: checkpoint/resume must be able to reopen the run before any `iteration_000/` artifact exists and still know baseline scoring is the active slot. Initialize `state.json.iteration_state` and `results.json.iteration_state` in that same write as `{"run_id":"[current_run_id]","run_path":"[current_run_path]","experiment_id":0,"active_phase":"eval","phase_status":"running","last_eval_status":null,"last_eval_results_ref":null,"next_action":"phase7_baseline_eval"}` so resume/dashboard consumers can read the runner's eval-vs-mutate handoff state directly instead of inferring it from directory scans. The same single iteration-start trigger must link the run record to the target skill/version under evaluation by capturing the working-copy skill path (`skill-under-test/SKILL.md`) plus the exact snapshot metadata for the version about to enter baseline scoring: if a derived kept version already exists, reuse that version label + source experiment; otherwise set `target_version.evaluation_label = baseline_candidate` and `source_experiment_id = null`. Always capture `target_version.snapshot_path = skill-under-test/SKILL.md` and `target_version.snapshot_sha256` from the exact working-copy skill file that baseline scoring will evaluate. Do not append a second run record during Experiment 0, later mutations, regression checks, or Session Close. After EACH experiment (including baseline), write 5 artifact files to `[workspace]/runs/run_<timestamp>/iteration_<NNN>/` — see `references.md > Iteration Directory Schema` for file formats. These files survive context compaction and session boundaries, making each experiment independently inspectable on disk. Iteration directories are written in both Full and Mini mode. Initialize `state.json.completion_cadence` if needed as part of the same run-start write, and re-scope it when necessary: default to `{"scope_type":"experiment_series","scope_id":"[current_run_path]","completed_experiments":0,...}` for this Phase 7 run, or reuse a previously chosen `scope_type:"skill"` counter if the campaign intentionally tracks cadence across multiple runs of the same skill. If `state.json.final_only_evaluation` exists from an older run and its `run_path` does not match the new `state.json.current_run_path`, clear it to null in the same run-start write so Session Close holdout validation for the new run starts from a clean idempotence marker. Log each write to session-log: `{"phase":"7","type":"iteration_write","experiment":N,"path":"runs/run_.../iteration_NNN/"}`, the single run-start event as `{"phase":"7","type":"iteration_run_started","run_id":"run_...","path":"runs/run_.../","target_version_label":"baseline_candidate|vN"}`, and any eval-to-mutate handoff as `{"phase":"7","type":"iteration_phase_transition","run_id":"run_...","experiment":N,"from":"eval","to":"mutate","last_eval_status":"completed","last_eval_results_ref":"runs/run_.../iteration_NNN/eval_results.json","next_action":"phase7_mutation_analysis"}` plus any mutate-to-test handoff as `{"phase":"7","type":"iteration_phase_transition","run_id":"run_...","experiment":N,"from":"mutate","to":"test","last_mutation_status":"completed","last_mutation_results_ref":"runs/run_.../iteration_NNN/mutation.md","next_action":"phase7_test_phase"}` plus any test-to-session_close handoff as `{"phase":"7","type":"iteration_phase_transition","run_id":"run_...","experiment":N,"from":"test","to":"session_close","phase_status":"ready","next_action":"phase7_session_close"}` and terminal runner outcomes as `{"phase":"7","type":"iteration_terminal_state","run_id":"run_...","phase_status":"completed|blocked","next_action":null}`.

**v4.2 challenger artifact contract:** When `challenger_mode` is enabled for a non-baseline experiment, keep the search topology bounded and copy-on-write. Use exactly 3 lane slots under the current iteration directory: `challengers/lane_01_incremental_cleanup/`, `challengers/lane_02_simplification/`, and `challengers/lane_03_from_scratch/`. Each lane works from the same current baseline candidate input for the active experiment, writes only inside its own lane directory, and must never edit the shared working-copy `skill-under-test/SKILL.md` in place while searching. Each lane directory must hold the lane-local candidate skill file, lane revision metadata, lane-local dev-scored evaluation output, and a concise lane summary. Persist the authoritative lane-selection result only in `[workspace]/runs/run_<timestamp>/iteration_<NNN>/challengers/shortlist.json`. That shortlist artifact may be referenced from `state.json` or `results.json`, but the full lane payload stays lane-local. Lane outputs are candidate artifacts only: rejected lanes never become first-class experiments or immutable version artifacts. The shortlist winner is the only challenger candidate that may enter the normal Experiment `N` path, become the canonical `candidate_skill_revision`, and later become an immutable kept version. Keep all lane search on the same trusted dev-scored surface already used by the mutation loop. Lanes may consume hydrated `parsed_meta_learnings`, `style_preferences.active_signals`, and `research-intake.md` when present, but lanes must not read `train`, `test`, or `adversarial_holdout`, must not bypass the active `mutation_stage_split_access_policy`, and must not introduce any alternate promotion surface. Automatic shortlist selection feeds the existing experiment presentation and user verdict confirmation flow only; it does not add a new human approval gate and it does not change the final-only Session Close holdout + `trust_gate` semantics. Keep the lane IDs exactly aligned with `references.md > Challenger Mode Configuration Schema`; do not mix prefixed lane slots in Gulf 3 with unprefixed lane IDs in the schema or tests.

1. **Pre-mutation baseline phase (mandatory):** Evaluate the unchanged working-copy skill before any mutation logic or candidate generation begins. Invoke the unchanged working-copy skill evaluation exactly three times for each baseline evaluation request. Treat those three invocations as the Experiment 0 baseline sample for that request; do not collapse them into one skill run with repeated judging. Persist each unchanged-skill baseline execution as a distinct record in `baseline_trials[]`. Assign every baseline trial record a unique `trial_index` (1-based) and stable `trial_id` so later baseline executions never overwrite earlier ones. Store each trial row with `run_index`, `timestamps`, `raw_outputs[]`, and `trial_metadata` so later comparisons can inspect what the unchanged skill produced on each input, when the run happened, and which dev corpus slice it used. Treat the ordered `baseline_trials[]` array as the baseline phase output contract for Experiment 0: downstream dashboards, reports, and retrieval consumers should expose those three separate trial rows directly, with mean/noise-floor metrics derived from the collection rather than replacing it with one aggregated baseline run. The root `noise_floor` summary remains derived from `baseline_trials[]`; do not replace it with a judge-only shortcut or a holdout-only variance estimate. Before Experiment 0 and every later Phase 7 split-scoped dataset read, route the request through `references.md > Restricted Mutation-Stage Dataset Access Path` using the active `mutation_stage_split_access_policy` from the run context. For baseline scoring, set `requested_operation = baseline_scoring` and `requested_split_id = dev`. The restricted path is the only allowed dataset-read route inside the mutation loop: it must enforce `references.md > Mutation-Stage Split Access Policy`. The policy still defines allowed split ids = `dev` only for baseline scoring, mutation scoring, mutation analysis, regression checks, and same-run version comparison. `train`, `test`, and `adversarial_holdout` are inaccessible during mutation-time operations. Canonicalize the caller's `requested_split_id` before checking the gate. If the raw token, an alias such as `holdout` / `adversarial-holdout`, or any delegated resolution path resolves to `adversarial_holdout`, reject the request as a blocked holdout access attempt. Reject the read if the requested operation is absent from `allowed_operations` or if the requested split is not in `allowed_split_ids`. If a mutation-stage request cannot be satisfied through that restricted path, stop with a blocking error instead of reopening files directly or falling back to an unrestricted read. Run baseline on the dev split fixtures only, score against all evals → Experiment 0. Record the active `input_set_id`, the exact `input_set_ref` (`input-sets.json#<set_id>`), and the finalized-order `input_ids` scored in this run with the experiment so later comparisons can prove they used the same scoring corpus and the same source inputs. `adversarial_holdout` is not part of the mutation loop scoring corpus; keep it hidden until Session Close evaluation-only validation. Record `eval_results` per eval, and require each decision to carry `pass_fail`, `reasoning_trace`, a structured `evidence` array, `supporting_items`, `weight`, `weight_source`, `weighted_points`, and `normalized_contribution`. `reasoning_trace` must be populated for every verdict as a concise, ordered explanation: `1) check the criterion`, `2) cite the concrete evidence`, `3) connect that evidence to the final Pass/Fail`. `evidence` must be a structured `evidence` array of objects (see `references.md > Judge Verdict Evidence Schema`). Required fields: kind, source, locator. Optional fields: `excerpt`, `metric`, or `artifact_ref`. `supporting_items` must capture the concrete intermediate judgment calls behind the verdict (see `references.md > Judge Decision Support Schema`). Each supporting item records the sub-decision, its local outcome, and `evidence_refs` pointing to the exact `evidence[]` objects used for that step. Roll these into a `decision_breakdown` object with `components` (ordered aggregation inputs for every eval used in the keep/discard math), `formula`, `weighted_points`, `total_weight`, `combined_score`, `combined_score_pct`, `threshold`, and `proposed_decision` (`baseline` for Experiment 0). Then derive `decision_explanation` from that `decision_breakdown`: store the final decision, a concise mixed-signal summary, and the strongest contributing eval outcomes with their impact on the final keep/discard. Then derive `mutation_handoff` using `references.md > mutation_handoff fields`: persist `normalized_evaluation_scores`, `failure_reasons[]`, and ordered `mutation_targets[]` in the exact structure Phase 7 step 2a expects, so mutate can consume the score/failure/target handoff without reverse-engineering it from prose. **Canonical heading parse:** parse the target skill's `##` headings into a list of section names. Log to session-log: `{"phase":"7","type":"canonical_headings","sections":["heading1","heading2",...]}`. This list is the denominator for `diversity_score` in the derived mutation registry (see `references.md > Derived Mutation Registry`). Before finalizing Experiment 0, materialize the unchanged working baseline into an immutable version artifact using `references.md > Skill Version Artifact Schema`. Write `[workspace]/skill-versions/<version_id>/SKILL.md` plus the paired `version.json`, set `version_id = skill_version__<current_run_id>__exp_000`, set `parent_version_id = null`, carry the resulting `version_artifact` object unchanged into both `iteration_000/eval_results.json` and `results.json.experiments[0]`, and update `[workspace]/skill-versions/lineage.json` in the same write so the baseline root version is queryable as the start of the lineage graph. Never overwrite an existing `skill-versions/<version_id>/` directory. **Write baseline iteration artifacts** to `iteration_000/`. Once those artifacts exist, baseline is finalized: increment `state.json.completion_cadence.completed_experiments` exactly once. Update the same `state.json.iteration_state` and `results.json.iteration_state` to `{"run_id":"[current_run_id]","run_path":"[current_run_path]","experiment_id":0,"active_phase":"mutate","phase_status":"ready","last_eval_status":"completed","last_eval_results_ref":"runs/.../iteration_000/eval_results.json","next_action":"phase7_mutation_analysis"}` so the runner records the completed eval output/status and automatically advances the same `run_*` into the mutate phase without manual bridging, new run IDs, or a second run directory. Then set `requires_human_spot_check = true` iff the updated `state.json.completion_cadence.completed_experiments` value is divisible by 3 (every 3rd completed experiment; otherwise false), copy the updated cadence snapshot into the root of `results.json`, copy the same finalized cadence snapshot plus `requires_human_spot_check` into Experiment 0, and append `session-log.json` entries of type `iteration_phase_transition` and `completion_cadence`. If `requires_human_spot_check` is true, resolve the current `evaluation_metadata.config.human_spot_check_calibration.sample_count` (default `2`, validation `>= 1`) and queue `[workspace]/checkpoint-tasks/exp0-human-spot-check.json` using `references.md > Human Spot-Check Task Schema`. The queued task must explicitly preserve `selection_strategy = priority_order`, the ordered `selection_priority[]`, and a dev-only sample set; never surface `adversarial_holdout` examples, holdout-derived evidence, or any other evaluation-only corpus through a cadence-triggered human spot-check task. Do not enter step 2a, generate candidates, or hypothesize mutations until Experiment 0 is finalized.
2. LOOP:
   a. Analyze failures → hypothesize ONE change. If the loaded run context does not already hold `style_preferences`, rebuild it from `state.json.mid_session_preference_signals` + `state.json.mid_session_preference_signals_path` using `references.md > Style Preferences Payload` before hypothesis generation. Treat `style_preferences.active_signals` as the canonical machine-readable mid-session preference ledger for this run, and consume that hydrated list directly when steering the next mutation hypothesis. Do not rescan `pending_user_override_scan`, raw `mid_session_preference_signals`, or `[workspace]/preferences.md` to recover preference rules once `style_preferences` is loaded. Use `style_preferences.resolved_preferences_path` only when the human-readable `[workspace]/preferences.md` wording must be cited or shown to the user, and still do NOT propose mutations that contradict learned user preferences. If the loaded run context does not already hold the bootstrap meta-learnings context, resolve `state.json.meta_learnings_path` (or the default AutoRefine skill directory copy), normalize the current target context, and hydrate `{meta_learnings_path, target_context, parsed_meta_learnings}` using `references.md > Campaign Bootstrap Meta-Learnings Context`. Then use `parsed_meta_learnings` from that bootstrapped object before generating mutation hypotheses using `references.md > Parsed Meta-Learnings Runtime Schema`. Only apply `actionable_entry_ids` from that bundle as cross-campaign steering input; candidate, superseded, rejected, or context-mismatched entries stay visible for humans but do not automatically steer the current run. If `[workspace]/research-intake.md` exists, read it next only when `state.json.research_intake.status` is `completed` or `partial`; treat the extracted patterns as candidate mutation sources, not mandatory edits or surrogate eval results. Before any research-derived donor pattern becomes a revision candidate, materialize a `mutation_candidate_evaluation` artifact using `references.md > Mutation Candidate Evaluation Schema`; it must capture the extracted donor pattern, the exact source evidence, target-skill similarity, and trust/overfitting signals so the promote/hold/reject call is auditable. Read the current run's `selected_eval_strategy_id` in `references.md > Skill Pattern Eval Strategy > Strategy Definitions`. Use `selected_eval_strategy_id` as the Phase 7 mutation priority and canonical mutation path instead of generic edits. **Read prior iteration decisions** from the current run directory (`decision.md` files in the current run directory — path from `state.json.current_run_path`) for full keep/discard reasoning that survives context compaction.

**Raw trace access (NEW — Meta-Harness pattern):** The mutation proposer MAY selectively grep/cat prior `iteration_*/eval_results.json`, `iteration_*/traces.md`, and `iteration_*/mutation.md` files when the `mutation_handoff` summary is ambiguous OR when investigating a suspected regression OR when a failure pattern resembles a prior experiment's discard autopsy. Use `grep -r "<pattern>" <current_run_path>/iteration_*` to find specific failure signatures across the run. Raw traces preserve diagnostic detail that summaries compress away — consult them for causal reasoning, not just when summaries are missing.

This is ADDITIVE to the existing `mutation_handoff`-based path, not a replacement. For most mutations, the `mutation_handoff` summary is sufficient. Use raw traces when:
- A mutation is a regression and you need to isolate the confound (which prior edit caused which behavior?)
- A failure pattern looks similar to a prior discarded experiment — check the discard_autopsy.classification
- The `mutation_handoff.failure_reasons[]` summary is vague (e.g., "output quality low") and you need the full failing output to understand what "low" means here.

Also read the latest finalized `mutation_handoff` block from the current run's `eval_results.json` artifact (or the mirrored experiment payload) and treat `mutation_handoff.normalized_evaluation_scores`, `mutation_handoff.failure_reasons[]`, and `mutation_handoff.mutation_targets[]` as the canonical machine-readable eval-to-mutate handoff. When `mutation_handoff` is present, start from its ordered `mutation_targets[]` list instead of rebuilding target priority from free-form critique text. Each `mutation_targets[]` row must already be actionable for step 2a: carry `target_id`, `target_location`, `recommended_mutation_type`, `priority`, `source_eval_ids`, `source_reason_ids`, `strategy_alignment`, `rationale`, and `expected_effect` straight into the one-change hypothesis instead of inferring them from prose. After selecting the next target row, convert the structured eval-to-mutate handoff into exactly one candidate `SKILL.md` revision using `references.md > Mutation Candidate Revision Artifact`; persist the selected target unchanged and write the full revised skill body into `candidate_skill_revision.content` before scoring or user presentation so the mutation artifact itself carries the generated candidate revision. Then materialize that generated candidate into a new immutable version artifact using `references.md > Skill Version Artifact Schema`: write `[workspace]/skill-versions/<version_id>/SKILL.md`, write the paired `version.json`, set `version_id = skill_version__<current_run_id>__exp_<NNN>`, set `parent_version_id` to the currently active baseline version artifact, carry the resulting `version_artifact` object unchanged into `mutation.md`, `eval_results.json`, and the finalized experiment record, and update `[workspace]/skill-versions/lineage.json` in the same write so the new candidate is recorded as a child of the source version. Immediately after that immutable snapshot exists, emit `references.md > Mutation Test Launch Payload` and persist it in `mutation.md > Test Launch Payload`. That payload is the canonical mutate-to-test bootstrap surface: it must carry `candidate_version`, `source_artifact_refs`, `eval_artifact_refs`, and `test_bootstrap_metadata` so the test phase can reopen the same candidate snapshot and frozen eval corpus without reconstructing them from scans. The lineage store must keep `child_version_ids[]` on the source node and make the full lineage path queryable for later traversal. Never overwrite an existing `skill-versions/<version_id>/` directory or mutate an older version artifact after it is written. **If recent experiments were discarded, read their `discard_autopsy` classification** — avoid same target if `wrong_target`, try different params if `wrong_params`, try different mutation type (add<->delete<->modify) if `wrong_type`. If failure analysis needs to reopen fixture content, scored inputs, or per-input outputs, route that read through `references.md > Restricted Mutation-Stage Dataset Access Path` by re-applying the active `mutation_stage_split_access_policy` with requested operation = `mutation_analysis` and keeping the read on the stored dev-scoped `input_set_id` + `input_ids` only. Candidate generation must obtain any split-scoped corpus view exclusively from that accessor. Do not read `fixtures-manifest.md`, `input-sets.json`, or any other direct split loader from the mutation candidate path. **Check search diversity:** compute the derived mutation registry (see `references.md > Derived Mutation Registry`). If `diversity_score < 0.5`, prioritize unexplored sections. Log the snapshot to session-log.

**Confound flagging (NEW — Meta-Harness pattern):** When hypothesizing the next mutation, the proposer MUST explicitly check for confounds with prior edits:
- If the proposed target section was also edited in the 2 most recent kept experiments → flag as a potential confound.
- If a prior kept experiment changed multiple sections in one mutation → flag as bundled.
- If a discarded experiment's `discard_autopsy.classification` was `wrong_target` for the same section → flag as target-saturation.

Add a 4th optional step to the reasoning_trace format for the mutation hypothesis only: `4) flag any confounds — if none, state "no confounds detected"`. This step is mandatory to emit when confounds exist; absent or "no confounds detected" is acceptable when none exist. Persist in `mutation.md > Mutation Rationale` alongside the existing 3-step reasoning_trace.

**Structured confound_check (forcing function):** Alongside the reasoning_trace, emit a structured block in `mutation.md > Mutation Rationale` that captures the 3 rule outcomes derivably, so "no confounds detected" is a derivable statement rather than a claim:

```json
{
  "confound_check": {
    "recent_edit_overlap": {
      "checked_experiments": ["exp_N-1", "exp_N-2"],
      "target_sections_edited_recently": ["section_id_list"],
      "overlaps_current_target": true|false
    },
    "bundled_edit": {
      "checked_experiment": "exp_N-1",
      "section_edit_count_in_that_experiment": <int>,
      "bundled": true|false  // true if count > 1
    },
    "target_saturation": {
      "checked_discards": ["exp_N-1 (if discarded)", "exp_N-2 (if discarded)"],
      "prior_wrong_target_classifications_for_current_target": <int>,
      "saturated": true|false  // true if any prior wrong_target on same section
    }
  }
}
```

Populate from `[workspace]/runs/run_*/iteration_*/mutation.md` (for prior edits) and `iteration_*/eval_results.json` `discard_autopsy.classification` (for target-saturation). If all 3 outcomes are false, the 4th reasoning_trace step may say "no confounds detected" and the proposer has the evidence to back the claim. If any outcome is true, the reasoning_trace 4th step must describe the confound concretely.

**Save backup** (`[workspace]/<skill>-optimized-prev.md`) → mutate a copy
      **v4.2 challenger_mode (Phase 7 instruction slice):** When `challenger_mode` is enabled, treat the step-2a mutation search as a bounded research tournament before the normal experiment path continues. Use exactly the 3 bounded copy-on-write lane slots defined above. `lane_01_incremental_cleanup` should make the smallest targeted revision that follows the ordered `mutation_targets[]`. `lane_02_simplification` should pursue deletion, compression, or boundary repair while keeping the same objective and eval target. `lane_03_from_scratch` is reserved for the plateau-breaker path and must remain dormant unless the v1 in-run plateau trigger fires; if it is dormant, still record the lane as skipped in its lane summary and in the later `research-memory.json` lane roster so replay/reporting can distinguish a dormant lane from a missing artifact. The plateau-breaker may activate `lane_03_from_scratch` only from in-run signals: either no kept post-baseline candidate exists yet in the active run after at least 2 finalized challenger experiments, or the 2 most recent discarded experiments share the same dominant `discard_autopsy.classification`. Do not trigger the plateau-breaker from holdout outcomes, cross-run history, or any Session Close artifact. Every active lane must start from the same current baseline version, use the same dev-only `input_set_id` and `input_ids`, and reuse the same trusted dev-scored surface already approved for the current experiment. Lanes may vary the candidate revision strategy, but they must not vary the evaluation corpus, must not peek at holdout data, and must not create alternate keep/discard math. After writing the lane-local candidate revisions, run the same dev-scored mutation evaluation surface on each active lane candidate and write the lane-local results under that lane directory. Then compute the automatic shortlist in `challengers/shortlist.json` using only the existing mutation-time evidence: dev score, noise-relative improvement vs the current baseline, regression status, and stability/disagreement notes already available from the trusted dev-scored surface. The shortlist winner is the only lane output promoted into the canonical Experiment `N` candidate artifact, the canonical `mutation.md`, and the normal step-2b through step-2f flow. Rejected lanes remain lane-local for forensics only. Do not ask for a separate shortlist approval. Feed the selected shortlist winner directly into the existing experiment presentation and user verdict confirmation flow so the user still sees one experiment-level decision point per experiment.
   b. **Score mutation (with bias reduction for agent-as-judge evals):** Before reading fixture content or scored inputs for this experiment, route that read through `references.md > Restricted Mutation-Stage Dataset Access Path` by re-applying the active `mutation_stage_split_access_policy` with requested operation = `mutation_scoring`. Then re-apply the active `mutation_stage_split_access_policy` with requested operation = `mutation_scoring`; do not materialize blocked splits while scoring. Intermediate scoring must resolve the scoring split exclusively through the active `mutation_stage_split_access_policy`. Do not branch on raw split IDs, reopen split manifests, or bypass the accessor to recover the dev corpus. If `requested_operation = mutation_scoring` resolves to `adversarial_holdout`, explicitly deny the request and fail closed before reopening any stored dev corpus.
      - For **code-based evals**: run directly (deterministic, no bias risk).
      - For **agent-as-judge evals**: the agent that hypothesized the mutation is biased toward finding it improved. Reduce this bias using the strongest available mechanism:

        **Tier 1 — Subagent dispatch (strong isolation, use when available):**
        Write `[workspace]/eval-tasks/exp{N}-E{M}.md` containing ONLY: the judge prompt (from `judges/`), the fixture input, and the skill output to evaluate (the mutated output — judge scores ONE output per invocation, same format as Phase 6 validation). Do NOT include: baseline output, mutation hypothesis, or Phase 1-3 findings. Dispatch a subagent with ONLY this file as input. Subagent produces Critique + Pass/Fail. Parent reads the verdict.

        **Fork economics (Tier 1 only):** When dispatching eval subagents, omit `subagent_type` to trigger a fork instead of a spawn. The fork inherits the parent's cached prompt prefix — judge prompts, fixtures, and workspace context are already loaded. Only the eval-task file content differs per experiment. This means each eval subagent pays cache price (1/10) for the shared prefix and full price only for the new eval-task content (~500 tokens). With 5 agent-as-judge evals across 5 experiments = 25 dispatches, fork saves ~80% vs spawn. If fork is unavailable (in-house agent), fall back to Tier 2.

        **Tier 2 — Behavioral instruction (bias reduction, for Read/Write/Bash-only agents):**
        If subagent dispatch is unavailable: score each agent-as-judge eval by reading ONLY the judge prompt file and the skill output. Before scoring, explicitly state: "I am now evaluating this output against the rubric only. I am disregarding my prior reasoning about why this mutation was made." This is a *heuristic* that reduces but does not eliminate self-certification bias.

        Keep eval-task files in `[workspace]/eval-tasks/` for debugging (clean up at Session Close, not per-eval). Record `eval_results` per eval, preserving the final `pass_fail`, full `reasoning_trace`, the structured `evidence` array, and `supporting_items` for every verdict, alongside `weight`, `weight_source`, `weighted_points`, and `normalized_contribution`. For evals tagged multi-judge, also persist a `multi_judge` block (see `references.md > Multi-Judge Verdict Schema`) with the independent panel verdicts, `agreement_rule: unanimous`, `consensus_status`, and any disagreement log reference. Only agreed multi-judge verdicts may contribute to `decision_breakdown`. If the panel disagrees, persist `pass_fail: disagreement`, leave `weighted_points` / `normalized_contribution` unresolved, append `` `multi_judge_disagreement` `` to session-log as `{"phase":"7","type":"multi_judge_disagreement","experiment":N,"eval":"E{N}"...}`, and surface the eval for human review instead of letting one judge silently choose the score. Build `evidence[]` using the `Judge Verdict Evidence Schema` so each verdict carries stable `kind`, `source`, and `locator` fields plus any needed excerpt, metric, or artifact metadata. Build `supporting_items[]` using the `Judge Decision Support Schema` so every material judgment call is captured separately with its own outcome and `evidence_refs` back to the exact evidence objects that supported it. Whether the verdict came from Tier 1 or Tier 2, normalize the judge critique into the same ordered `reasoning_trace` shape: `1) check the criterion`, `2) cite the concrete evidence`, `3) connect that evidence to the final Pass/Fail`. Every material clause in `reasoning_trace` must have a matching supporting item, even when multiple clauses lead to the same final verdict.

**Domain metric scoring (NEW — v4 effectiveness criteria):** If `state.json.domain_eval_config_path` is not null and `[workspace]/domain-eval/golden-set.jsonl` exists and has at least one row:
- Run the mutated skill on each golden set input (no change to skill invocation; the mutated skill reads the input and produces output).
- Execute the configured `eval_script_path` with (input, skill_output, expected_output) — the script returns a score 0-1 per input.
- Aggregate scores using the metric aggregator defined in the script (e.g., mean for NDCG@5, precision@k, etc.).
- Add an entry to `decision_breakdown.components[]` with:
  - `eval_id: "domain-metric"`
  - `category: "domain-metric"`
  - `weight: <base_weight> * <weight_multiplier>` — the effective weight is already multiplied per `references.md > Domain Eval Config Schema`. Example: base 1.0 × multiplier 2.0 → `weight: 2.0`. This keeps the canonical `weighted_points = weight * pass_fail_score` formula consistent with every other component.
  - `weight_multiplier: <from config.json>` — kept as a provenance-only field (default 2.0), documenting how the effective `weight` was derived. Downstream math uses `weight` directly; do NOT re-multiply.
  - `weighted_points: weight * pass_fail_score` — canonical formula, matches all other components. For a domain-metric eval with `weight=2.0` and `pass_fail_score=score` (0-1 continuous), `weighted_points` = `2.0 * score`.
  - `weight_source: "domain_eval_config"`
  - `pass_fail`: derived from `threshold_pass` and `threshold_concern` (see Phase 5 Step 1 domain-metric threshold rules)
  - `reasoning_trace`: 1) the computed score, 2) the threshold comparison, 3) the Pass/Fail derivation
  - `evidence[]`: each golden-set input that contributed to the score, with `kind: "metric"`, `source: "domain_eval"`, `locator: <input_id>`, `metric: <per-input score>`

If `domain_eval_config_path` is null OR golden-set.jsonl is missing/empty: skip this block entirely (no domain-metric component in combined_score — current v4.0 behavior).

As soon as scoring completes for the experiment, update the same run-scoped `state.json.iteration_state` and `results.json.iteration_state` with the current `experiment_id`, `last_eval_status = "completed"`, and `last_eval_results_ref = "runs/.../iteration_<NNN>/eval_results.json"` before regression checks or user presentation. When the mutate phase finishes, record `last_mutation_status = "completed"` and `last_mutation_results_ref = "runs/.../iteration_<NNN>/mutation.md"` on that same iteration-state object, ensure `mutation.md > Test Launch Payload` contains the stored `phase7_mutation_to_test_launch` contract, then automatically advance the same `run_*` into test with `active_phase = "test"`, `phase_status = "ready"`, and `next_action = "phase7_test_phase"`. When test-phase checks complete for that same experiment, automatically advance the same run into Session Close with `active_phase = "session_close"`, `phase_status = "ready"`, and `next_action = "phase7_session_close"`. At Session Close completion, set terminal success as `active_phase = "session_close"`, `phase_status = "completed"`, and `next_action = null`; on unrecoverable closeout failure, set terminal failure as `active_phase = "session_close"`, `phase_status = "blocked"`, and `next_action = null`. Keep the existing `run_id` / `run_path` unchanged across all transitions, and resume from persisted `iteration_state` so eval->mutate->test->session_close progression continues automatically without manual handoff. `decision_breakdown.components[]` is the canonical aggregation breakdown field: it must capture the intermediate per-eval math (`weight`, `weighted_points`, `normalized_contribution`), and the aggregate fields (`weighted_points`, `total_weight`, `combined_score`, `combined_score_pct`, `threshold`, `proposed_decision`) must match the keep/discard recommendation shown to the user. In the same scoring pass, derive `decision_explanation` from `decision_breakdown` plus the final recommendation: persist the strongest contributing eval outcomes, tag whether each one supports keep or discard, record each outcome's impact on the final keep/discard, and synthesize a balanced explanation that names both positive and negative pressure when both exist.
   **2b.1 — Efficiency tracking (NEW — v4 effectiveness criteria):** For each experiment (including baseline), record efficiency metrics in the eval_results.json artifact alongside the verdict:
- `token_count`: total tokens consumed by the skill execution across all inputs scored in this experiment
- `tool_call_count`: total number of tool calls made across all inputs
- `execution_time_ms`: wall-clock time for skill execution across all inputs

Persist efficiency at TWO granularities in `eval_results.json`:
- `efficiency_metrics.aggregate`: experiment-level totals with fields `token_count`, `tool_call_count`, `execution_time_ms`
- `efficiency_metrics.per_input[]`: one row per scored input with fields `input_id`, `token_count`, `tool_call_count`, `execution_time_ms`

Phase 1b floor reads `per_input[]` to compute per-example statistics (median, p95, max) against the threshold. The `aggregate` block stays for dashboard totals and quick scans. Both are derivable from the same instrumentation — cheap to capture now, expensive to add after baseline artifacts exist without them.

Do NOT include these fields in `combined_score` — they are informational in v4.x, surfaced in the Aggregation Explainer and Session Close.

These metrics feed the Phase 1b effectiveness floor (dimension 5: efficiency) on re-runs — Phase 1b compares against `baseline_trials[].trial_metadata` from Phase 7 Experiment 0 to compute efficiency pass/concern/fail.

   c. **Regression check:** Compare current `eval_results` against prior kept experiments. If any eval that previously passed now fails → regression detected. Details: `references.md > Regression Check Schema`. Skip on experiments 0 and 1 (baseline has no prior kept experiments to compare against). If this check needs to reopen scored inputs, fixture text, or joined per-input outputs, first route that read through `references.md > Restricted Mutation-Stage Dataset Access Path` by re-applying the active `mutation_stage_split_access_policy` with requested operation = `regression_check`. Then re-apply the active `mutation_stage_split_access_policy` with requested operation = `regression_check` and keep the read on the stored dev-scoped `input_set_id` + `input_ids` only.
   d. **Present to user** — show mutation diff, score change, regression status, and the Aggregation Explainer format (see `references.md > Aggregation Explainer Template`). The explainer must show every eval, its verdict, its `weight_source`, the normalization step (`weighted_points / total_weight`), the final `combined_score`, and the keep/discard threshold used for the recommendation. The explainer rows must match `decision_breakdown.components` 1:1 so the persisted record stays audit-ready. Also show the stored `decision_explanation` summary and strongest contributing eval outcomes so the user can see which outcomes most influenced the recommendation and how they impacted the final keep/discard. If you also show a version-to-version or baseline-to-mutation before/after view, first apply `references.md > Version Comparison Alignment` as a comparison preflight: require the same `input_set_id`, verify the two experiments contain the exact same set of stable `input_id`s, and surface any `missing_from_left`, `missing_from_right`, `extra_in_left`, or `extra_in_right` IDs before showing a diff. Treat any reopened per-input output or joined comparison payload as requested operation = `same_run_version_comparison` through `references.md > Restricted Mutation-Stage Dataset Access Path` under the active `mutation_stage_split_access_policy`. That comparison request still uses requested operation = `same_run_version_comparison` under the active `mutation_stage_split_access_policy`; if that read would cross into a blocked split, reject the comparison. If the comparison preflight fails, reject the comparison or return a flagged `invalid-comparison` status with the mismatch payload and stop there; you must not emit normal comparison results, delta rows, or before/after score summaries from a mismatched pair. Only after that preflight passes may you join per-input changes by stable `input_id`, never by display order. One decision point.
   e. User accepts or overrides → record in results.json (with `input_set_id` + `input_set_ref` + finalized-order `input_ids` + `eval_results` + `decision_breakdown` + `decision_explanation` + `regression_check`) + results.tsv + changelog.md. Every experiment in the run must preserve the same `input_set_id`, `input_set_ref`, and `input_ids`; if the scoring corpus changes, stop and start a new run instead of mixing versions scored on different inputs. When a mutation in this run applied one or more actionable meta-learning IDs, append one `results.json.meta_learning_outcomes[]` row per applied ID immediately after scoring using the current run's before/after quality signals (`before` = previous experiment snapshot, `after` = current experiment snapshot) plus helpful score/label and the frozen criteria snapshot from `references.md > Meta-Learning Outcome Record Schema`. Resolve the helpful score/label by running the configured-criteria evaluator for each applied meta-learning ID: read the captured `quality_signals.delta`, apply the row's criteria snapshot in binary or graded mode, and persist the resulting determination without replacing the snapshot post hoc. Also persist `results.json.meta_learning_audit_records[]` for every curated meta-learning considered in the current run, not just the applied IDs. That audit ledger must capture one row per curated rule with applied/skip status, evidence metrics from the parsed entry, and a helpfulness verdict (`evaluated` with score/label when applied; `not_evaluated` when skipped). The keep/discard threshold applies to `decision_breakdown.combined_score`, not raw pass count. If **kept**: assign a version label (see Version Registry below) and **write iteration artifacts** to `iteration_<NNN>/` now (verdict is final), then increment `state.json.completion_cadence.completed_experiments` exactly once, persist the updated root cadence in `results.json`, set `requires_human_spot_check` on this experiment from the updated cadence snapshot (true on every 3rd completed experiment; otherwise false), copy the same finalized cadence snapshot onto this experiment record, and log `{"phase":"7","type":"completion_cadence",...,"status":"keep"}`. If `requires_human_spot_check` is true, resolve the current `evaluation_metadata.config.human_spot_check_calibration.sample_count` (default `2`, validation `>= 1`) and queue `[workspace]/checkpoint-tasks/exp{N}-human-spot-check.json` using `references.md > Human Spot-Check Task Schema`. That queued task must preserve `selection_strategy = priority_order`, the ordered `selection_priority[]`, and dev-only surfaced samples; do not let cadence-triggered human review surface `adversarial_holdout` examples or any holdout-only evidence path. When the updated `state.json.completion_cadence.completed_experiments` value is divisible by 3 and `state.json.iteration_state.phase_status` remains `running` or `ready`, also queue or update the root `pending_user_override_scan` hook plus `[workspace]/checkpoint-tasks/exp{N}-user-override-scan.json` using `references.md > User Override Scan Task Schema`; the scan window is the last 3 finalized experiments in the active refinement loop. If **discarded**: do NOT write or increment yet — step 2f writes after autopsy and only then finalizes the experiment.
   f. If discarded (regression or user choice): **discard autopsy first** (reads from `experiments[]` in results.json, not the skill file), then restore backup as current baseline. Classify why using `references.md > Discard Autopsy Heuristics` — `wrong_target` (section unlikely to respond), `wrong_params` (right section, wrong approach), or `wrong_type` (add/modify/delete mismatch). Record in `experiments[].discard_autopsy` and session-log: `{"phase":"7","type":"discard_autopsy","experiment":N,"classification":"...","reasoning":"1-sentence"}`. For kept experiments, `discard_autopsy` is null. **Write iteration artifacts** to `iteration_<NNN>/` now (includes autopsy classification). Once that write succeeds, the discard is finalized: increment `state.json.completion_cadence.completed_experiments` exactly once, persist the updated root cadence in `results.json`, set `requires_human_spot_check` on this experiment from the updated cadence snapshot (true on every 3rd completed experiment; otherwise false), copy the finalized cadence snapshot onto this experiment record, and log `{"phase":"7","type":"completion_cadence",...,"status":"discard"}`. If `requires_human_spot_check` is true, resolve the current `evaluation_metadata.config.human_spot_check_calibration.sample_count` (default `2`, validation `>= 1`) and queue `[workspace]/checkpoint-tasks/exp{N}-human-spot-check.json` using `references.md > Human Spot-Check Task Schema`; the queued payload must carry the resolved `sample_count`, preserve `selection_strategy = priority_order`, and continue to sample from dev-scored examples only. When the updated `state.json.completion_cadence.completed_experiments` value is divisible by 3 and `state.json.iteration_state.phase_status` remains `running` or `ready`, also queue or update the root `pending_user_override_scan` hook plus `[workspace]/checkpoint-tasks/exp{N}-user-override-scan.json` using `references.md > User Override Scan Task Schema`; the scan window is the last 3 finalized experiments in the active refinement loop.
   g. **Circuit breaker check** — see below
3. Repeat until all evals pass, budget exhausted, or circuit breaker stops the loop

**Circuit breaker:**
Track `consecutive_discards` in state.json (integer, starts at 0). Update **after step (e)**, using the final keep/discard outcome:
- Experiment **kept** (by agent or user override) → reset `consecutive_discards = 0`
- Experiment **discarded** (regression, low score, or user override to discard) → increment `consecutive_discards += 1`

Disabled in Mini mode (`quick_start.completed = true` AND `gates.gulf_1 = "pending"`). Discard autopsy (step 2f) runs in all modes including Mini. Reset to 0 on Phase 7 re-entry after loop-back.

If `consecutive_discards >= 3`: STOP mutations and classify:

1. **Content ceiling** — if current baseline score > 80% AND all 3 discarded experiments scored within 5 percentage points of the current baseline:
   → "Your skill scores [X]%. The last 3 mutations all scored [Y-Z]%, unable to push past baseline. Either the skill is done, or add harder eval fixtures targeting dimensions not yet covered."

2. **Strategy review needed** — all other cases. Present the raw data:
   → "3 consecutive discards. Here's the data for your review:"
   - Last 3 experiments: scores, sections targeted (`changes[].location`), eval results, discard reason (regression / low score / user override), AND `discard_autopsy` classification
   - If all 3 autopsy classifications are `wrong_target`: "All 3 targeted unresponsive sections. Explore untried sections or add harder eval fixtures."
   - If all 3 are `wrong_params`: "Right sections but wrong approach. Try fundamentally different mutation strategies (e.g., rewrite vs. tweak, subtractive vs. additive)."
   - If all 3 are `wrong_type`: "Mutation type mismatch. Reverse direction: if adding, try deleting; if modifying, try replacing entirely."
   - If mixed or < 5 total experiments: present the autopsy breakdown and note: "Mixed signals — review the pattern before continuing."
   - If >= 5 total experiments with no clear autopsy pattern: "Possible causes: evals can't discriminate (scores flat across experiments), or judge noise (scores swing >15pp between adjacent experiments)."
   → "Recommendation: review eval-suite.md. Consider looping back to Phase 5-6, targeting different SKILL.md sections, or accepting current quality."

Report format (only show "Continue?" if budget remains):
Compute the derived mutation registry before presenting the diagnosis (see `references.md > Derived Mutation Registry`). Log the snapshot to session-log.

```
Circuit breaker: 3 consecutive discards
  Best score achieved: [X]% (Experiment [N])
  Diagnosis: [content ceiling | strategy review needed]
  Search diversity: [diversity_score] ([N]/[M] sections explored)
  Autopsy pattern: [summary of last 3 discard_autopsy classifications]
  Evidence: [raw data points including discard reasons]
  Recommendation: [specific action]

  [If budget remains] Continue anyway? (not recommended) / Stop and address diagnosis?
  [If budget exhausted] Budget exhausted. Review the diagnosis above.
```

If user continues: reset `consecutive_discards = 0` and resume the mutation loop (do NOT exit Phase 7). If breaker triggers again: "Circuit breaker triggered a second time (triggered_count: 2). The pattern of repeated discards suggests the current approach isn't working. Consider ending Phase 7." Still allow user to override.

Record in state.json: `circuit_breaker: {triggered_count: N, last_experiment: N, diagnosis: "..."}`. Record in session-log.json: `{"phase":"7","type":"circuit_breaker","diagnosis":"...","consecutive_discards":3,"experiments":[...]}`.

**After circuit breaker — two paths:**
- If user chose **stop**: exit the mutation loop. Phase 7 is done (early termination). Proceed to Loop-Back Prompt check, then Session Close.
- If user chose **continue**: reset counter, resume mutation loop at step (a). Do NOT proceed to Loop-Back Prompt yet — that only runs when the loop fully ends.

**Key rules:** One mutation per experiment. Mutate a copy (`[workspace]/<skill>-optimized.md`), not the workspace baseline. If score improves, the mutated copy becomes the new baseline for the next experiment. Target baseline 60-80% (>90% = evals too easy). **Mutation types:** add, modify, OR delete. After every 2-3 additive mutations, try one subtractive mutation — remove instructions and measure if performance improves. Shorter skills often outperform bloated ones. Formats: `references.md > Results & Changelog Schemas`.

**Confidence-weighted scoring:** Weight each eval by its judge's validated TPR/TNR. Code evals = 1.0. Agent evals = (TPR+TNR)/2. Experiment score = weighted sum / sum of weights. Persist this as `decision_breakdown`: each eval stores `weight`, `weight_source`, `weighted_points`, and `normalized_contribution`; the aggregate stores `formula`, `weighted_points`, `total_weight`, `combined_score`, `combined_score_pct`, `threshold`, and `proposed_decision`. In Mini mode, use the same structure with simplified weight sources (`mini_mode_code_default`, `mini_mode_agent_discount`).

**User verdict confirmation:** After each experiment, present the score change, regression status, and proposed keep/discard to the user. If the user overrides (e.g., keeps despite regression, or discards despite clean score), log as `type: "judge_gap"` in session-log.json: `{"phase":"7","type":"judge_gap","experiment":N,"agent_verdict":"keep","user_verdict":"discard","reason":"..."}`. These indicate judge blind spots and feed the loop-back prompt. Regression overrides also log: `{"phase":"7","type":"regression",...,"user_action":"keep_override"}`.
At every third completed experiment inside an active refinement loop, run the session-level user override scan from `references.md > User Override Scan Task Schema` over the last 3 finalized experiments. Copy `decision_breakdown.proposed_decision` into each scan row as `agent_verdict`, copy the finalized experiment status as `user_verdict`, and mark `override_detected = true` only when those keep/discard verdicts differ. Every scan execution must also write or refresh `state.json.mid_session_preference_signals` and `results.json.mid_session_preference_signals` from that scan's latest outcome (`not_detected`, `pending_confirmation`, `confirmed`, `applied`, or `skipped`), replace the stored `source_task_ref`, `source_window_experiment_ids`, counts, and `last_detected_at` with the current scan window, and mirror `[workspace]/preferences.md` into `mid_session_preference_signals_path` for the same run even before user confirmation so resume-time mutation steps can reopen the same human-facing ledger without guessing. If the current scan finds no reusable rule, persist `signals: []` with the refreshed status and counts instead of leaving stale confirmed entries in place as if they belonged to the new window.

**Eval dimension pruning:** After the loop completes, check results.json for evals that passed 100% across ALL experiments (baseline + mutations). Flag them: "E{N} passed every experiment. This eval may no longer discriminate — consider whether the model has outgrown it or the criterion is too lenient."

**Mutation history:** Record discarded experiments with the same detail as kept ones — full `changes[]` diff, `eval_results`, and `regression_check`. Discarded mutations have diagnostic value: patterns in what fails reveal what the skill actually needs.

**State:** Update after each experiment (include `eval_results` and `regression_check`). **Dashboard:** serve workspace with `python3 -m http.server 8080`.

### Verdict Explanation Cards (v4.0)

The verdict evidence system ensures every judge verdict is transparent and inspectable. Each `eval_results[]` record preserves: `pass_fail`, `reasoning_trace` (3-step: criterion check, evidence citation, verdict link), structured `evidence[]` array (kind/source/locator), and `supporting_items[]` (intermediate judgments with `evidence_refs` back to evidence). Multi-judge evals also preserve `multi_judge` consensus metadata so disagreement remains inspectable instead of being collapsed into one opaque score. Schemas: `references.md > Judge Verdict Evidence Schema`, `references.md > Judge Decision Support Schema`, and `references.md > Multi-Judge Verdict Schema`.
Attach the extracted `supporting_items[]` to the same verdict object, do not carry supporting items forward to another eval, and preserve them in the final judge output written to `eval_results[]` and iteration `eval_results.json`.
Do not mirror `holdout_score`, `holdout_gap`, or per-variant holdout rows into top-level state.

### Human Spot-Check Judgment Capture (v4.1)

When any queued calibration sample is actually reviewed, persist one `human_review_judgment` object per surfaced sample using `references.md > Human Review Judgment Schema`. The stored record must carry the surfaced judge verdict snapshot, the human decision (`pass_fail`, `decision_type`, `agreement`, `rationale`, `reviewer_notes`), reviewer identity, review timestamps, and audit metadata. If the review is tied to a concrete experiment, append it to that experiment's `human_review_judgments[]`; if it also appears in a completed task artifact, mirror the exact same object there. Do not rely on free-form `spot_check` session-log lines as the authoritative calibration record.

### Version Registry (v4.0 — derived view)

The version registry is a **derived view** from `results.json`, not a separate file. It provides human-facing version labels for kept experiments while preserving the stored immutable `version_artifact.version_id` and `version_artifact.snapshot_path` on each finalized experiment.

**Version numbering:**
- `v0` = baseline experiment (always exists, experiment id 0)
- `v1` = 1st kept mutation, `v2` = 2nd kept mutation, etc.
- Discarded experiments do NOT get version numbers (they remain in results.json for forensics)

**Computation (on demand):**
1. Read `results.json.experiments[]`
2. Filter for `status = "keep"` or `status = "baseline"`
3. Sort by `id` (experiment sequence number)
4. Assign version labels: v0 for baseline, vN for Nth kept experiment

**When to compute:** Any time version data is needed — after a kept mutation (show "Kept as v3"), at Session Close (show v0 → vN progression), or when the user requests version comparison.

Schema details: `references.md > Version Registry Schema`.

---

## Loop-Back Prompt

Phase 7 ends when: budget exhausted, all evals pass, user stops, OR circuit breaker fires and user chose stop. All exit paths trigger this check.

**Final-only evaluation stage trigger:** Only after Phase 7 fully completes for the active `state.json.current_run_path` and the user declines any loop-back may you trigger the final-only evaluation stage. Trigger it exactly once per run, immediately before the rest of Session Close. It is outside Experiment 0 and step 2a-2g. Never run it during baseline scoring, intermediate mutation scoring, regression checks, per-experiment keep/discard handling, or circuit-breaker diagnostics. If Session Close resumes after this stage already completed for the active run, reuse the stored result instead of rerunning it.

If session-log has >=2 `judge_gap` entries:

> "You overrode N experiment verdicts, suggesting judge blind spots:
> - [reason 1]
> - [reason 2]
> Loop back to Phase 5 to refine judges, then re-run Phase 7? Or accept current results?"

If user loops back: Phase 5 enters append mode (`locked_judges` prevents modifying approved judges). Phase 6 validates only new judges. Phase 7 re-runs with expanded suite — reset `consecutive_discards = 0` in state.json on re-entry (scoring changed, old counter is invalid). Score may drop — this is more accurate measurement, not regression. Explain this to the user. Max 2 loop-backs per session. Increment `loop_iteration` in state.json.

---

## Session Close

Runs after Phase 7, when user stops mid-pipeline, or when user explicitly pauses. Minimum: session-log must have >=3 entries for learning summary.

0. **Apply back gate** — If any mutations were kept: first sync the best kept mutation into the working copy (`cp [workspace]/<skill>-optimized.md [workspace]/skill-under-test/SKILL.md`) so the working copy reflects the latest improvements. Then ask: "Apply the improved SKILL.md back to the original location ([original-skill-path])? (y/n)". If yes: `cp [workspace]/skill-under-test/SKILL.md [original-skill-path]/SKILL.md`. If no: tell the user where the improved version lives. Log the decision to session-log.json: `{"type":"apply_back","applied":true/false,"source":"[workspace]/skill-under-test/SKILL.md","target":"[original-skill-path]/SKILL.md"}`. **If the copy fails** (e.g., sandbox restriction): don't retry. Print the full path and tell the user to copy manually: `cp [workspace]/skill-under-test/SKILL.md [original-skill-path]/SKILL.md`.
0b. **Clean up temporary files** — Remove `[workspace]/eval-tasks/` directory if it exists (debugging artifacts from Phase 7 verification isolation). Do NOT remove `[workspace]/runs/` — iteration directories are the forensic record and are retained indefinitely.
0c. **Adversarial holdout validation (final-only evaluation stage)** — Enter this stage only after Phase 7 fully completes for the active `state.json.current_run_path` and any loop-back prompt has been resolved in favor of ending the run. Trigger this stage once, immediately after the mutation loop is over and before other Session Close cleanup/reporting. Do not call it from Experiment 0, step 2a-2g mutation iterations, regression checks, circuit-breaker branches, or any other intermediate mutation iteration branch. If `fixtures-manifest.md` exposes a split with `split_id=adversarial_holdout`, explicitly stop using the active `mutation_stage_split_access_policy` and switch to the evaluation-only `session_close_holdout_validation` scope before reading holdout fixtures. Do not route holdout reads through `references.md > Restricted Mutation-Stage Dataset Access Path`; that accessor is mutation-stage-only and must stay dev-scoped. If `state.json.final_only_evaluation.run_path` already matches `state.json.current_run_path` with `status = "completed"`, reuse the stored holdout result instead of rerunning it. Otherwise, restore the completed version lineage from `references.md > Version Registry Schema` (baseline + kept variants only, in version order) and run each completed variant through the existing variant-evaluation interface on the holdout split. The final evaluation runner must preserve the same stored result shape AutoRefine already uses for versioned evaluations: `input_set_id`, `input_set_ref`, `input_ids`, `score`, `max_score`, `pass_rate`, `final_score`, `evaluation_metadata`, `evaluation_metadata_validation`, `eval_results`, `decision_breakdown`, and `decision_explanation`. Populate that per-run score surface before persisting the structured holdout result artifact so every `variant_results[]` row stays self-contained. Do not invent a second holdout-only result shape or a Session Close-only scorer. Persist the ordered holdout outputs using `references.md > Final Holdout Variant Runner`. Persist the ordered holdout outputs inside the dedicated `session_close_holdout/variant_results.json` artifact so Session Close storage stays separate from mutation-time scoring data while each `variant_results[]` row still reuses the normal evaluation record shape. If holdout evaluation fails or exits early, still write `session_close_holdout/variant_results.json` with the run-level `status`, a coarse `reason`, structured `failure_reasons[]`, and any partial `variant_results[]` already finalized so every resolved final-only run has one machine-readable artifact. Do not append Session Close holdout rows to mutation-time `results.json.experiments[]`, iteration `eval_results.json`, or any other mutation-loop record. Persist `evaluated_experiment_ids` on `state.json.final_only_evaluation` so the runner records every completed variant it scored. Persist `variant_results_ref` on `state.json.final_only_evaluation` so Session Close resume and downstream comparison views can reopen the holdout artifact without reconstructing it. Then write `state.json.final_only_evaluation = {"run_path":"[current_run_path]","stage_id":"session_close_holdout_validation","status":"completed|skipped|failed|aborted","triggered_after_loop":true,"triggered_from":"session_close_step_0c","evaluated_experiment_id":<best_kept_or_baseline_id>|null,"evaluated_experiment_ids":[<baseline_and_kept_ids>],"variant_results_ref":"runs/.../session_close_holdout/variant_results.json","reason":"<resolved_exit_reason|null>"}`. Use `status:"failed"` when the evaluator reaches a terminal error after entering the stage, and `status:"aborted"` when it exits early before finalization can complete cleanly. Inside `session_close_holdout/variant_results.json`, keep the selected final candidate summary authoritative: `selected_candidate_summary` should carry `version`, `experiment_id`, and `holdout_score` only. Write `trust_gate` into that same artifact as the authoritative promotion surface for the selected candidate; do not duplicate `trust_gate` into `state.json.final_only_evaluation`. Label `trust_gate.holdout_assessment.interpretation_mode = directional_only` when `holdout_n < 10`, otherwise `decision_grade`. `trust_gate.outcome = promote` requires `holdout_score >= baseline_holdout_score + combined_score_noise_threshold`, zero hard blockers, and zero advisory flags. Set `trust_gate.outcome = block` when unresolved contributing disagreements remain, when an unapproved material judge remains in the selected candidate path, or when any other enumerated hard blocker in `references.md > session_close_holdout/variant_results.json` is triggered. When the selected candidate clears hard blockers but the holdout remains `directional_only`, the result stays within the noise floor, or calibration lands in `review_required` under the stored `false_pass_rate` / `false_fail_rate` thresholds, set `trust_gate.outcome = review_required`. Emit any dev-side tuning metrics (for example `dev_score` and `holdout_gap`) under a separate sibling `optimization_metrics` section labeled non-authoritative so dashboards and production consumers can inspect tuning signal without mistaking it for the published final result. Do not mirror `holdout_score`, tuning diagnostics, per-variant holdout rows, or `trust_gate` into top-level state; those machine-readable outputs belong only in the dedicated `session_close_holdout/variant_results.json` structure. Treat it as evaluation-only: do not reopen Phase 5/6/7 refinement from these examples inside the same run. Append a session-log entry that records the ordered holdout run, the selected experiment id, the artifact ref, and the final `trust_gate.outcome`; downstream consumers should reopen the dedicated holdout artifact for numeric score/gap details instead of reading duplicated state fields. If no `adversarial_holdout` split exists for the run, write the same `state.json.final_only_evaluation` object with `status:"skipped"`, `evaluated_experiment_ids":[]`, `variant_results_ref:"runs/.../session_close_holdout/variant_results.json"`, and `reason:"no_adversarial_holdout_split"` so Session Close still records that the final-only stage was resolved exactly once for this run.
0d. **Session Close spot-check task** — Queue `[workspace]/checkpoint-tasks/session-close-human-spot-check.json` using `references.md > Human Spot-Check Task Schema`. Resolve `sample_count` from `evaluation_metadata.config.human_spot_check_calibration.sample_count` (default `2`, validation `>= 1`), then apply the mandatory closeout floor: `sample_count = max(sample_count, 5)`. Write that resolved value into the queued task payload with `trigger:"session_close"` and `minimum_sample_floor:5` so the closeout calibration task is explicit and reproducible. This task still reviews dev-scored surfaced samples only: preserve `selection_strategy = priority_order`, keep the ordered `selection_priority[]`, and never surface `adversarial_holdout` examples or holdout-derived evidence through Session Close calibration.
0e. **Run-local research-memory artifact (v4.2):** After the final-only holdout stage is resolved for the active run, but before the human-readable learning summary is synthesized, write `[workspace]/runs/run_<timestamp>/session_close/research-memory.json` for the active `state.json.current_run_path`. This artifact is a run-local machine-readable summary of the Phase 7 challenger search, not a new trust or promotion surface. It should record the lane roster for each finalized challenger experiment, lane activation/skipped status, lane mutation family, shortlist winner, shortlist rejection reasons, whether the plateau-breaker was triggered, which run-local research inputs were actually consumed (`research-intake.md`, actionable curated meta-learning IDs, and style-preference signals), and whether the selected candidate's dev gains survived the existing final trust review. Keep the boundary explicit: this research-memory artifact complements the existing learning summary and the existing `meta_learning_outcomes[]` / `meta_learning_audit_records[]`; it does not replace them. Do not auto-promote raw run-local research memory into curated cross-campaign meta-learnings, do not mutate `parsed_meta_learnings` from it during the same run, and do not treat it as a holdout or `trust_gate` artifact. Curated cross-campaign promotion remains a separate reviewed process outside this Session Close step.

### Contract Effectiveness Report (NEW — v4 effectiveness criteria)

If `state.json.contract_status = "confirmed"`, generate a contract-based effectiveness section in the Session Close report. Add to `session-close-report.md`:

```
## Contract Effectiveness

Success examples:    [N/3 pass]  [list which passed/failed — e.g., "success-1 pass, success-2 pass, success-3 concern (output missing key section)"]
Failure mode catch:  [N/3 caught] [list which caught/missed — e.g., "failure-1 caught, failure-2 caught, failure-3 missed (skill didn't flag the ambiguity)"]
Trigger precision:   [N/3 correct fires, N/3 correct declines, N/X phrasing variations correct] 
Domain metric:       [metric_name: score / threshold — e.g., "NDCG@5: 0.72 (threshold 0.65) — pass"] or "not configured"
Efficiency trend:    [baseline_token_count] → [final_token_count] tokens, [baseline_tool_calls] → [final_tool_calls] tool calls

Floor delta (Phase 1b → final):
  activation_quality: [start status] → [end status]
  outcome_quality: [start status] → [end status]
  [... for each dimension that changed ...]
```

**How to generate:**
1. Re-run the final kept skill version on all 9 contract examples (3 success, 3 failure, 3 DNT).
2. Score each example against its contract criterion:
   - Success examples: judge output against `output_shape.description` using a fresh agent-as-judge call
   - Failure examples: check if the skill caught the failure mode (flagged, declined, or recovered)
   - Do-not-trigger examples: check if the skill correctly declined/routed/ignored
3. If `domain_eval_config_path` is set: re-run domain metric on the golden set with the final version.
4. If `state.json.effectiveness_floor` is populated: re-run Phase 1b dimensions and compute delta.
5. Report efficiency: compare baseline experiment (iteration_000) token/tool counts to final kept experiment's counts.

This answers: "Is the skill more effective now than when we started, measured against the author's own definition of effective?" — complementary to `combined_score`, which measures judge-perceived quality.

### Leakage Audit (NEW — Meta-Harness pattern)

After Session Close selects the winning version, run a leakage audit:

1. Load the winning version's `SKILL.md` content.
2. For each fixture in the `test` split and `adversarial_holdout` split (from `fixtures-manifest.md`), extract the raw input text.
3. Check if any test/holdout input text appears verbatim in the winning `SKILL.md` (use substring match on spans of 20+ contiguous characters).
4. Flag matches in the Session Close report:

```
## Leakage Audit

Test split strings in SKILL.md: [N matches / 0 expected]
  [list each match: "match X: <first 40 chars of matched span>..."]
Adversarial holdout strings in SKILL.md: [N matches / 0 expected]
  [list each match]

Status: [CLEAN / WARNING / FAIL]
```

Status rules:
- CLEAN: zero matches in both splits
- WARNING: 1-2 matches (may be coincidental for common phrases like "see example below")
- FAIL: 3+ matches, or any match of 50+ contiguous characters (likely memorized overfit)

A FAIL leakage audit does NOT automatically discard the winning version, but surfaces the issue prominently. The author decides whether to accept or re-run with different fixtures.

### Re-evaluation Reminder (NEW — Resolver pattern)

Append to the Session Close report:

```
## Next Steps

- **Review the final skill** at `[original_skill_path]/SKILL.md` after Apply Back.
- **Re-run AutoRefine in 30-90 days.** Trigger descriptions and skill behavior drift as real usage evolves — skills that pass today may fail new user phrasings 90 days from now. Re-evaluation catches description/behavior drift early.
- **Monitor production usage** if possible — unexpected phrasings, missed activations, or user corrections are strong signals that a re-run is due sooner.
```

### Version Comparison Summary (v4.0)

Before showing any baseline → final or Version N → Version N+1 results, apply the version comparison protocol:

1. **Compute version registry** — derive v0/v1/.../vN labels from results.json (see Version Registry above).
2. **Run comparison preflight** — read `references.md > Version Comparison Alignment` first. Require the same `input_set_id`, verify the two experiment records contain the exact same set of stable `input_id`s, and report any `missing_from_left`, `missing_from_right`, `extra_in_left`, or `extra_in_right` IDs if the sets do not line up. If the comparison preflight fails, mark the summary as `invalid-comparison` and stop; do not emit normal comparison results.
3. **Show side-by-side comparison** using the format from `references.md > Version Comparison Template`:
   ```
   Version Comparison: v0 -> vN
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   Eval         v0        vN        Delta
   ────────────────────────────────────────────────────
   E1 (code)    Pass      Pass      --
   E2 (agent)   Fail      Pass      improved
   E3 (agent)   Pass      Fail      REGRESSED
   ────────────────────────────────────────────────────
   Pass rate    60.0%     80.0%     +20pp
   Weighted     62.3%     78.5%     +16.2pp
   Shared inputs  7 trusted improved / 2 trusted regressed / 0 unreliable / 11 unchanged
   ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

   Skill diff (v0 -> vN):  [expandable diff]
   ```
   The comparison result must also report `shared_input_summary` from the joined `per_input[]` rows: `total_shared_inputs`, `improved`, `regressed`, `unreliable`, and `unchanged`. Treat `improved` and `regressed` as the trusted buckets once a baseline reliability threshold is available; route within-noise-floor rows to `unreliable` instead of counting them as trusted change.
   If either compared version is the Experiment 0 baseline, also carry `left_baseline_trials[]` or `right_baseline_trials[]` into the comparison payload. Reuse the same serialized `baseline_trials[]` rows from the baseline experiment and surface the baseline-side trial collection inline before the shared-input diff so the user can inspect the three pre-mutation runs with their per-trial raw outputs, scores, run indexes, timestamps, and trial metadata.

### Per-Category Score Report (v4.0)

If eval category tags are present (see `references/gulf2-specification.md > Phase 5`), report separate scores per category:
```
Score by category:
  task-completion: 100% (3/3 evals passing)
  quality:          75% (3/4 evals passing)
  structural:      100% (2/2 evals passing)
```

### Remaining Session Close Steps

1. **Save checkpoint** — update `state.json.checkpoint` with current state and write `resume-prompt.txt` to workspace root. See `references.md > Checkpoint Schema`. Append to session-log: `{"type":"checkpoint",...}`.
2. **Synthesize** session-log.json into 3-5 bullet learning summary (what worked, what was overridden, patterns emerged). When `research-memory.json` exists for the active run, use it as structured source material for the summary, but keep the human-readable summary concise and reviewable instead of dumping the full machine-readable artifact inline.
3. **User curates** — present summary, ask for edits or approval
4. **Persist** to agent memory system (path from `state.json.memory_path`, or ask on first run and record). If no memory system exists, write to `[workspace]/learnings.md` as fallback. Persist the curated summary, not the raw `research-memory.json` payload, unless a separate reviewed workflow explicitly promotes parts of that run-local artifact into curated cross-campaign memory.
5. **Present resume prompt** — "You can resume anytime. Paste this into a new session:" followed by the resume prompt from `resume-prompt.txt`.
6. **Archive** — rename session-log.json to `session-log-<timestamp>.json`

If <3 entries: still save checkpoint (step 1), skip learning summary. Say: "Not enough data for a learning summary yet. Checkpoint saved — you can resume later."

**Phase boundary checkpoints:** At every phase completion (not just Session Close), update `state.json.checkpoint` and write `resume-prompt.txt`. This is automatic — no user interaction needed. Log to session-log.
